name: Mi primer Workflow de Hola Mundo

run-name: ¡Ejecución de ${{ github.actor }}! 🚀

on: [push]

jobs:
  hola-mundo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Definir variable dinámicamente
        # 'github.actor' devolverá "ccaceres-dev" automáticamente
        run: echo "VAR_NOMBRE=${{ github.actor }}" >> $GITHUB_ENV

      - name: Correr script de Python
        run: python hola.py
